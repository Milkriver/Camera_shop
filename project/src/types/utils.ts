export type TSortType =
    {
      type: string;
      name: string;
      label: string;
    }

export type TFilterItem = {
      type: string;
      name: string;
      checked: boolean;
      title: string;
    }
